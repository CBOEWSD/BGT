{{#if widget.title}}
  <h3 class="widget-title {{#each widget.classes}} {{this}} {{/each}}">
    {{widget.title}}

    {{#if widget.promo}}
    <div class="promo">
      <a
      href="{{widget.promo.url}}"
      class="{{#if widget.promo.class}}icon-logo {{widget.promo.class}}{{/if}}
      {{#each widget.promo.classes}} {{this}} {{/each}}">
      {{{widget.promo.text}}}
    </a>
    </div>
    {{/if}}
  </h3>
{{else}}
  {{! return blank}}
{{/if}}