<div class="widget-list simple {{#each this.classes}} {{this}} {{/each}}">
  {{> widgets/widget-title}}

  <ul>
    {{#each items}}

      <li class="{{#each this.classes}} {{this}} {{/each}}">
        {{#if this.link}}
          <a href="{{this.link.url}}" title="{{this.text}}">
        {{/if}}

        {{this.text}}

        {{#if this.link}}
          </a>
        {{/if}}
      </li>

    {{/each}}
  </ul>
</div>